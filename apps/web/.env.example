# App URL (used for metadata and sitemap generation)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# API mocking (development only)
# Set to "enabled" to use MSW service worker for mock API responses.
# Remove or leave unset to call the real API.
# NEXT_PUBLIC_API_MOCKING=enabled
